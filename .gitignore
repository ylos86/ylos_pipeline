# Bytecode Python (genere par les imports : CLI, tests, Python embarque de Blender)
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Bundle redondant des sources (export/transfert, regenerable)
files003.zip

# Reglages locaux Claude Code (overrides machine, non partages)
.claude/settings.local.json

# Backups auto de sauvegarde .hdanc (Houdini en cree un nouveau a chaque overwrite du meme
# fichier) : bruit, regenerable via tools/houdini/build_publish_hda.py.
plugins/houdini/otls/backup/

# Config de lancement locale Claude Code (chemin absolu machine)
.claude/launch.json
